%if False
\begin{code}
open import Relation.Binary
module ExampleProof where
\end{code}
%endif
\begin{code}
zero-annihilates : {X : Setoid} (0# : X) (_∙_ : X → X → X) → (∀ x → 0 ∙ x)
zero-annihilates = ? 
\end{code}
